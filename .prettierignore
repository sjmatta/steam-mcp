dist/
coverage/
node_modules/
package-lock.json

# Byte-for-byte copies of files Steam wrote. Reformatting them would defeat
# their purpose: they exist to pin the exact quirks of Steam's own output
# (concatenated JSON documents, stringified inner JSON, tab-indented VDF).
test/fixtures/
